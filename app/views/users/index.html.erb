<%= link_to "new", new_user_path %>
<ul>
    <% if @users; @users.each do |user| %>
    <li><%= user.username %>    <%= link_to "edit", edit_user_path(user) %> <%= link_to "delete", user_path(user), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
    <% end; end %>
</ul>